version: "3.9"
services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
  fast-meet:
    env_file:
      - client/.env
      - server/.env
    command: npm run deploy
    